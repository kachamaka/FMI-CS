import java.io.*;
import java.util.Random;
import java.util.Scanner;

public class vands {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int count = sc.nextInt();
		int opportunity = 0;
		for (int j = 0; j < count; j++) {
			int[] week = new int[] { 0, 0, 0, 0, 0, 0, 0 };
			for (int i = 0; i < 12; i++) {
				Random random = new Random();
				int current = random.nextInt(7);
				week[current]++;
			}
			if ((week[0] != 0) && (week[1] != 0) && (week[2] != 0)
					&& (week[3] != 0) && (week[4] != 0) && (week[5] != 0)
					&& (week[6] != 0))
				opportunity++;
		System.out.println( week[0]+ "  "+week[1]+ "  "+ week[2]+ "  "+ week[3]+ "  "+ week[4]+ "  "+ week[5]+ "  "+ week[6]+ "  ");	
		}
		
		System.out.println("вероятността е " + opportunity + "/" + count);
	}
}
